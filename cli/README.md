# ga4gh-community-stack (Python package)

The **`lab-stack`** CLI package name is **`ga4gh-community-stack`**. It is **not** on PyPI (as of 2026-09-05). From this clone:

```bash
pip install -e "./cli[dev]"
lab-stack --help
```

Project documentation: [../README.md](../README.md)
