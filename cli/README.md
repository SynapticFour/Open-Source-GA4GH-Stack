# ga4gh-community-stack (Python package)

The **`lab-stack`** CLI is published as **`ga4gh-community-stack`** on PyPI (planned). From this monorepo:

```bash
pip install -e "./cli[dev]"
lab-stack --help
```

Project documentation: [../README.md](../README.md)
