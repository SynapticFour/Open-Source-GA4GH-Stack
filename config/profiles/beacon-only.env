# Profile: Beacon v2 + MongoDB + Caddy + oauth2-proxy (ELIXIR LS Login ready)
LAB_STACK_PROFILE=beacon-only
INCLUDE_BASE=true
INCLUDE_BEACON=true
INCLUDE_WES=false
INCLUDE_TES=false
INCLUDE_DRS=false

MONGO_PASSWORD=changeme
HOST=localhost
TLS=false

# ELIXIR LS Login — replace for production; demo uses skip-auth regex when LAB_STACK_DEMO=1
LS_LOGIN_CLIENT_ID=replace-me
LS_LOGIN_CLIENT_SECRET=replace-me
COOKIE_SECRET=change-me-to-32-byte-secret-hex
