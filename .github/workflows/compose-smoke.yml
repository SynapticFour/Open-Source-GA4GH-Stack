name: Compose smoke

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  beacon-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install CLI
        run: pip install -e "./cli"

      - name: Generate compose
        run: |
          lab-stack generate compose \
            --config config/profiles/beacon-only.env \
            --output /tmp/ga4gh-cs

      - name: Validate compose file
        run: docker compose -f /tmp/ga4gh-cs/docker-compose.generated.yml --project-directory /tmp/ga4gh-cs config

      - name: Start Beacon + MongoDB
        run: |
          docker compose -f /tmp/ga4gh-cs/docker-compose.generated.yml --project-directory /tmp/ga4gh-cs up -d beacon mongodb

      - name: Wait for Beacon service-info
        run: |
          set +e
          for i in $(seq 1 72); do
            curl -fsS http://localhost:5050/ga4gh/beacon/v2/service-info >/dev/null && exit 0
            echo "[wait] Beacon not ready yet (attempt ${i}/72), sleeping 5s"
            sleep 5
          done
          echo "Beacon did not become ready within 360s"
          exit 1

      - name: Beacon diagnostics on failure
        if: failure()
        run: |
          docker compose -f /tmp/ga4gh-cs/docker-compose.generated.yml --project-directory /tmp/ga4gh-cs ps
          docker compose -f /tmp/ga4gh-cs/docker-compose.generated.yml --project-directory /tmp/ga4gh-cs logs --no-color beacon mongodb || true

      - name: Tear down
        if: always()
        run: |
          docker compose -f /tmp/ga4gh-cs/docker-compose.generated.yml --project-directory /tmp/ga4gh-cs down -v
