{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "beacon.fullname" . }}-conf
data:
  conf.py: |
    # Placeholder — replace via lab-stack generate or kubectl edit
    import logging
    import yaml
    beacon_id = "lab.beacon.demo"
    beacon_name = "Beacon"
    api_version = "v2.2.0"
    uri = "http://localhost:5050"
{{- end }}
