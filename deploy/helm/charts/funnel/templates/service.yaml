{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-funnel
spec:
  selector:
    app: funnel
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: grpc
      port: 9090
      targetPort: 9090
{{- end }}
