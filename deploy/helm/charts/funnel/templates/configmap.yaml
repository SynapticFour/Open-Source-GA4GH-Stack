{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-funnel-conf
data:
  funnel.conf: |
    Server:
      HostName: "0.0.0.0"
      RPCPort: "9090"
      HTTPPort: "8000"
{{- end }}
