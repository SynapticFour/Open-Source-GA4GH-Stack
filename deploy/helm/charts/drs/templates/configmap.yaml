{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-drs-config
data:
  drs.config.yml: |
    serverProps:
      publicApiPort: 4500
      adminApiPort: 4501
    dbProps:
      url: "jdbc:sqlite:/db/drs.db"
      username: ""
      password: ""
{{- end }}
