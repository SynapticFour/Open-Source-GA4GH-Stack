{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-drs
spec:
  selector:
    app: drs
  ports:
    - name: public
      port: 4500
      targetPort: 4500
    - name: admin
      port: 4501
      targetPort: 4501
{{- end }}
